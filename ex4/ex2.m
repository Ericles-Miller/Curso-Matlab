%Implemente uma função que gere um gráfico para a função peaks (disponível no MATLAB).
% Utilize o intervalo entre -5 e 5 para X e Y

function ex2()
    z = linspace(-5,5);
    x = z;
    y = z;
    peaks(x,y);
end